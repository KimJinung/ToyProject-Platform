spring:
  profiles:
    active: local
  datasource:
    url: jdbc:mysql://localhost:3306/platform
    username: root
    password: example
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create

    properties:
      hibernate:
        format_sql: true

logging:
  level:
    sql: DEBUG

